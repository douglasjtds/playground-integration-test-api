meta {
  name: Patch Task Status
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/tasks/:id/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: {{taskId}}
}

body:json {
  {
    "status": "IN_PROGRESS"
  }
}
