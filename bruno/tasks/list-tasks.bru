meta {
  name: List Tasks
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/tasks
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  ~projectId: {{projectId}}
  ~status: TODO
  ~priority: HIGH
}
