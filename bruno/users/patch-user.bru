meta {
  name: Patch User (PATCH)
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/users/:id
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: {{userId}}
}

body:json {
  {
    "name": "John Patched"
  }
}
